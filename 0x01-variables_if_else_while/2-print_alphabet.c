#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - A program that that prints the alphabet in lowercase
 * Return: always 0
 */

int main(void)
{
	putchar(65);
	putchar('\n');
	return (0);
}
